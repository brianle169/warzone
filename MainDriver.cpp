#include <iostream>
#include "Cards.h"
using namespace std;

int main() {

    // Meeting questions:

    // Do we need comments in the cpp if we already have them in the header? 
    // Initialization list
    // Should vectors themselves be pointers or hold pointers? 
    // 4.3.2?
    return 0;
}
